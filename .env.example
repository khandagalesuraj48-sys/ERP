# MILESTONE ERP — Environment Configuration
# Construction Machinery & Mechanical ERP
#
# The application runs cleanly in offline/local mode when these are unset.
# Provide these only when connecting a clean new Supabase database.

# --- Supabase (Client / Browser) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
